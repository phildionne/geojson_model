require 'spec_helper'

describe GeojsonModel do
  it 'has a version number' do
    expect(GeojsonModel::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
